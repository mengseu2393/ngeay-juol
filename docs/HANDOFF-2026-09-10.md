# Handoff — 2026-09-10

Live handoff for the next session. **Not a historical spec** (unlike `ai-tasks/*.md`,
`TASK_PWA.md`, `prompt.md` — see CLAUDE.md). Delete or supersede this file once the
open items below are done.

## State

- Branch `main`, working tree **clean**, **nothing pushed**.
- Full suite green: `php -d memory_limit=1G vendor/bin/phpunit` → **296 tests, 1176 assertions**.
- 24 commits ahead of `18bd71b` (the last commit from before this session).

## What shipped today

Earlier in the session — 14 commits splitting a 104-file uncommitted backlog
(QR login, Docker, batch-PDF queue, security guards, i18n, CSS). Then 10 more:

| Commit | What |
|---|---|
| `43635ef` | delete unreachable `UtilityWaiverResource`; fix CLAUDE.md test command |
| `3e16b26` | admin: assign a subscription when a landlord is created |
| `22a3b4b` | admin: support reads subscriptions, billing actions stay super-admin |
| `2556b2f` | admin: approve/reject payments from the payments list |
| `6b19077` | landlord: record payment in one click + 58mm/A5 receipt |
| `ef0d783` | landlord: move-out workflow with deposit settlement |
| `45921ac` | landlord: issue tenant portal login from the tenancy screen |
| `348eefc` | landlord: wire up move-in completion + fix dropped side effects |
| `6f2c1e0` | landlord: tenant directory |
| `6c569a4` | i18n: 107 new km.json keys |

New tables: `deposit_settlements`, `deposit_deductions`. New columns on `rentals`
(`move_out_date`, `moved_out_at`, `moved_out_by_id`, `move_out_reason`).

## Open items — start here

1. **Khmer translation pass.** The 107 keys added in `6c569a4` are **English
   placeholders**. Nothing renders as a raw missing key, but the Khmer UI shows
   English for every new surface (deposit settlement, move-out, receipts, tenant
   directory, landlord provisioning). Needs a native pass — the deposit/settlement
   vocabulary especially.
   Find them: `git show 6c569a4 -- lang/km.json | grep '^+'`

2. **Migrations have never run against MySQL.** Tests are SQLite `:memory:`. The
   three new migrations read as MySQL-safe but are unverified. Run against the dev
   DB before trusting them — mind `rentals.active_unit_id` (STORED GENERATED,
   MySQL-only).

3. **`TenantResource` duplicates `UserResource`'s visibility rule** verbatim (both
   carry a docblock tying them together). Will drift. Clean fix: extract a
   `App\Filament\Concerns\ScopesToVisibleUsers` trait — requires editing
   `UserResource`.

4. **Dead code found, not removed** (verify before deleting; some are cited by CLAUDE.md):
   - `app/Services/MoveInPaymentAllocator.php` — still zero call sites even after move-in was wired
   - `app/Services/MoveInPaymentRequestService.php` — same
   - `resources/views/filament/pages/consumption-history.blade.php` — no matching page class exists
   - `resources/views/reports/utilities-pdf.blade.php` — unreferenced, but CLAUDE.md cites it twice
   - `app/Filament/Resources/PropertyResource/RelationManagers/WaiversRelationManager.php` —
     `PropertyResource::getRelations()` returns `[]`
   - Cosmetic: `PropertyUtilityResource/RelationManagers/WaiversRelationManager.php:16` names
     the now-deleted `UtilityWaiverResource` in a comment

5. **`SubscriptionPaymentResource` row actions are tested through a harness**, not the
   live screen (its list page redirects on mount). The live path is covered via the widget.

## Bigger features still open — need your decisions

Deliberately out of scope today; each is a feature, not a fix:

- **Online rent payment** (portal is read-only). Which gateway — ABA / Wing / Bakong?
- **Tenant notifications** (nothing ever reaches the tenant; `TopDebtorsWidget`'s
  "who to phone" is literal). Telegram is the obvious channel here.
- **Expenses & per-property P&L** — income is tracked, costs are not, so "did this
  property make money" is unanswerable. Needs a chart-of-accounts decision.
- **Reports/exports** — income, collections, occupancy, arrears. Only the utility
  usage export exists.
- **Lease renewal** — `LeaseExpiryWidget` warns, then offers no renew action.
- **Impersonation / "view as landlord"** for support (zero impersonation code today).

Also still open from `docs/ROADMAP.md`: dunning delivery is counts-only, no payment
gateway automation, no auto-renewal charging.

## Commands worth remembering

```bash
php -d memory_limit=1G vendor/bin/phpunit            # full suite (artisan test OOMs — -d doesn't reach children)
php artisan test --filter=SomeTest                   # targeted is fine
vendor/bin/pint --dirty
grep -nE "rgb\(var\(--[a-z0-9-]+\) */ *[0-9.]+\)" public/css/rentwise-admin.css   # must return nothing
PHP_CLI_SERVER_WORKERS=6 /usr/bin/php8.3 artisan serve --port=8002
```

After touching an invoice/receipt PDF, generate one and check
`storage/logs/laravel.log` for `Browsershot invoice PDF render failed` — the dompdf
fallback is silent and mangles Khmer.
